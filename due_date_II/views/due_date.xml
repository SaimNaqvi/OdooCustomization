<odoo>
    <data>
        <record model="ir.ui.view" id="account_move_inherited_form_view">
            <field name="name">Account Move Inherited Form View</field>
            <field name="model">account.move</field>
            <!-- Paste the ref="external_id of the model in which you want it to run" -->
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">  
                    <data>
                        <xpath expr="//field[@name='invoice_date']" position="after">
                            <!-- Add new fields here -->
                            <!-- <field name="due_date_I"/> -->
                            <field name="due_date_II" invisible="1"/>
                        </xpath>
                        <xpath expr="//form[1]/sheet[1]/group[1]/group[2]/label[1]" position="attributes">
                            <attribute name="string">Due Date</attribute>
                        </xpath>
                    </data>
            </field>
        </record>
    </data>
</odoo>
